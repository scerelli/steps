/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import Steps from "./Steps";
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Steps);
